import AuthGuard from "@/common/components/AuthGuard"
import React from "react"
import { APP_NAME } from "@repo/constants"
import { Metadata } from "next"
import { ACCOMMODATION } from "@/common/constants"
import Message from "@/module/Accommodation/Message"

export const metadata: Metadata = {
  title: `${ACCOMMODATION} - ${APP_NAME}`,
  description: `Generated by ${APP_NAME}`,
}

const AccommodationMessagePage = () => {
  return (
    <AuthGuard>
      <Message />
    </AuthGuard>
  )
}

export default AccommodationMessagePage
